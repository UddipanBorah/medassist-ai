scikit-learn>=1.3
numpy>=1.24

# Optional - only needed if you want real LLM generation instead of the
# offline MockBackend. Install whichever provider you plan to use.
# openai>=1.0
# anthropic>=0.30
